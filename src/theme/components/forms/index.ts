import type { Components, Theme } from '@mui/material/styles';
import { selection } from './selection';

export const forms: Components<Theme> = {
  ...selection,
};

export { selection };

export default forms;
